/*
package com.ChallengesFerGomez.LiterAluraChallenge;

import java.sql.Connection;
import java.sql.DriverManager;
import java.sql.SQLException;

public class PostgreSQLTestConnection {
    public static void main(String[] args) {
        // URL de conexión a PostgreSQL: jdbc:postgresql://<HOST>:<PORT>/<DATABASE>
        String url = "jdbc:postgresql://localhost:5432/LiterAluraChallenge_DATABASE";
        String user = "literalurauser"; // Cambia por tu usuario
        String password = "Lufe3120$"; // Cambia por tu contraseña

        try (Connection connection = DriverManager.getConnection(url, user, password)) {
            System.out.println("¡Conexión exitosa a PostgreSQL!");
        } catch (SQLException e) {
            System.out.println("Error al conectar a la base de datos");
            e.printStackTrace();
        }
    }
}

*/
